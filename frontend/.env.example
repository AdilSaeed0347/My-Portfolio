# Set this in Vercel Project Settings -> Environment Variables.
# It is read at BUILD TIME by scripts/generate-env.js to produce env.js,
# which tells the static frontend where the backend API lives.
#
# Point this at your Render or Railway backend URL (no trailing slash):
BACKEND_URL=https://your-backend.onrender.com
